"""deerflow —— DeerFlow 的核心 Python 包。

该顶层包聚合了 DeerFlow Agent 系统所需的全部子模块，包括 agents、config、
models、sandbox、tools、skills、mcp、reflection、tracing、uploads、utils 等。
调用方通常通过 ``deerflow.client.DeerFlowClient`` 或 ``deerflow.runtime``
下的运行入口使用本包。
"""
